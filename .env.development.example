# Opcional: só se precisar outra URL/porta. Por padrão, em `npm run dev` já usamos a função em :8888.
# VITE_EXTRACT_ENTRY_URL=http://localhost:8888/.netlify/functions/extract-entry

# API Node local.
VITE_API_URL=http://localhost:3001
